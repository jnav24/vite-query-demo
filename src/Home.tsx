import React, { FC } from 'react';

const Home: FC = () => {
	return (
		<>
			<h1>Home</h1>
			<p>Fetch data page uses the standard API calls.</p>
			<p>Fetch query data page uses the react-query to make API calls.</p>
		</>
	);
};

export default Home;
